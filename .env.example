# ============================================
# DATABASE CONFIGURATION
# ============================================

# Primary database connection (direct connection)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# Example for local development:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/investor_db

# Connection pooling URL (recommended for production with services like Supabase, Neon, etc.)
# Uncomment and use this instead of DATABASE_URL in production for better performance
# DATABASE_URL=postgresql://user:password@pooler.region.provider.com:5432/investor_db?pgbouncer=true

# Alternative formats for different scenarios:
# Unix socket: postgresql://user:password@/investor_db?host=/var/run/postgresql
# With SSL: postgresql://user:password@host:5432/investor_db?sslmode=require
# With connection pool: postgresql://user:password@host:5432/investor_db?connection_limit=5&pool_timeout=10

# Prisma logging (set to 'true' in production to log queries)
PRISMA_LOG=false

# ============================================
# APPLICATION CONFIGURATION
# ============================================

# Environment: development | production | test
NODE_ENV=development

# Public application URL (used for API calls and redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# FILE UPLOAD SETTINGS
# ============================================

# Maximum file size in bytes (default: 3MB = 3145728 bytes)
# 5MB = 5242880, 10MB = 10485760
MAX_FILE_SIZE=3145728

# Upload directory path (relative to project root)
UPLOAD_DIR=./uploads

# Allowed file types (comma-separated MIME types)
ALLOWED_FILE_TYPES=application/pdf,image/jpeg,image/png,image/jpg

# ============================================
# SECURITY (Optional - Add as needed)
# ============================================

# JWT secret for authentication (generate with: openssl rand -base64 32)
# JWT_SECRET=your-super-secret-jwt-key

# Session secret
# SESSION_SECRET=your-session-secret-key

# ============================================
# EXTERNAL SERVICES (Optional - Add as needed)
# ============================================

# Email service (e.g., SendGrid, AWS SES)
# EMAIL_API_KEY=your-email-api-key
# EMAIL_FROM=noreply@yourdomain.com

# File storage (e.g., AWS S3, Google Cloud Storage)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=your-bucket-name
